---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: snapcast
      image: debian
      image_version: jessie
      ansible_groups:
        - group1
verifier:
  name: testinfra
